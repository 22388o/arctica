#this script will execute when the user clicks the 'continue' button on the first screen to flash SD 1
#here we will obtain the tails img, verify the signatures, create persistence & create admin password

echo "running initial flash"